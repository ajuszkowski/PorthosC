package mousquetaires.languages.syntax.xrepr.events.barrier;

import mousquetaires.languages.syntax.xrepr.events.XEventBase;
import mousquetaires.languages.syntax.xrepr.processes.XEventInfo;


public class XBarrierEvent extends XEventBase {

    // TODO
    public XBarrierEvent(XEventInfo info) {
        super(info);
    }
}
