package mousquetaires.languages.syntax.xrepr.types;

import mousquetaires.languages.syntax.xrepr.XEntity;


public abstract class XType implements XEntity {

    // probably base X type system on scalar & aggreate types:
    // Section 6.2.5 of the C11 standard explains: Arithmetic types and pointer types are collectively called
    // scalar types. Array and structure types are collectively called aggregate types.
}
